{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "DevServer",
            "program": "${workspaceFolder}/devserver.js"
        }
    ]
}